package com.github.geekuniversity_java_215.cmsbackend.core.utils;

import java.util.Set;
import java.util.function.Function;
import java.util.stream.Collectors;

public class CoreSecurityUtils {



//    public Set<String> toStringRoles(Set<UserRole> roles) {
//        return roles.stream().map(UserRole::getName).collect(Collectors.toSet());
//    }
}
